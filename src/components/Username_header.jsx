const Username_header = ({ username }) => {
  return (
    <div className="Username_header">
      <h2 className="Username">Logged in as: {username}</h2>
    </div>
  );
};

export default Username_header;
